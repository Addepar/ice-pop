{{unbound _parentFinder}}

{{#if renderInDOM}}
  {{#ember-popper
    popperTarget=popperTarget
    placement=placement
    modifiers=modifiers
    renderInPlace=renderInPlace
    registerAPI=registerAPI
    class=(concat "adde-animated " (if makeVisible "is-visible " "") popperClass)
  }}
    {{yield}}
  {{/ember-popper}}
{{/if}}

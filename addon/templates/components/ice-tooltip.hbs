{{#if isOpen}}
  {{#ice-popper
    id=_popperId
    target=_target
    placement=placement
    class="ice-tooltip"
    classNameBindings="isShowing:is-visible class"
  }}
    {{yield}}
    <div class="popper-arrow" x-arrow></div>
  {{/ice-popper}}
{{/if}}
